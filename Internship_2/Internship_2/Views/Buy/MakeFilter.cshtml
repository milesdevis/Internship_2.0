@model Internship_2.Models.CarImageModel

@{
    var d = (from i in Model select i.Description).ToArray();
    for (var i = 0; i < d.Length; i++)
    {
        if (d[i].Contains("MakeFilter"))
        {
            //CODE TO SORT
        }
    }
}